apiVersion: apps/v1
kind: Deployment
metadata:
  name: kraken-origin
spec:
  replicas: {{ .Values.origin.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: kraken
      app.kubernetes.io/component: origin
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kraken
        app.kubernetes.io/component: origin
        app.kubernetes.io/instance: {{ .Release.Name }}
      {{ with .Values.origin.annotations -}}
      annotations:
{{ tpl . $ | indent 8 }}
      {{- end }}
    spec:
      {{- if hasKey .Values.origin "hostNetwork" }}
      hostNetwork: {{ .Values.origin.hostNetwork }}
      {{- end }}
      {{ with .Values.origin.tolerations -}}
      tolerations:
{{ toYaml . | indent 6 }}
      {{- end }}
      {{- if .Values.origin.affinity }}
      {{ with .Values.origin.affinity -}}
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
{{ toYaml . | indent 12 }}
      {{- end }}
      {{- end }}
      {{ with .Values.origin.initContainers -}}
      initContainers:
{{ toYaml . | indent 6 }}{{ end }}
      imagePullSecrets:
      - name: nvcr.io
      containers:
      - name: main
        image: {{ .Values.origin.image }}
        imagePullPolicy: {{ .Values.kraken.imagePullPolicy }}
        command:
        - /bin/bash
        - -c
        - "sleep 30 && /usr/bin/kraken-origin --config={{ .Values.origin.config }} --blobserver-port=80 --peer-port=8080"
        volumeMounts:
        - name: config
          mountPath: /etc/config
{{ with .Values.origin.extraVolumeMounts }}{{ toYaml . | indent 8 }}{{ end }}
      volumes:
      - name: config
        configMap:
          name: kraken
{{ with .Values.origin.extraVolumes }}{{ toYaml . | indent 6 }}{{ end }}
---
kind: Service
apiVersion: v1
metadata:
  name: kraken-origin
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: kraken
    app.kubernetes.io/component: origin
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: {{ .Values.kraken.origin_port }}
